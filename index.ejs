

<% 
    var data = [ "foo", "bar", "baz" ];
%> 
<html>

<head>

    <link rel="stylesheet" href="assets/normalize.css" />

    <!--<% var names= ["index.css", "header.css"]; %>
    <% names.forEach(name=> { %>
    <link rel="stylesheet" href="<%= name %>">
    <% }); %>-->
    <script src="myscripts.js"></script>
    <link rel="stylesheet" href="index.css"> 
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@700&family=Playfair+Display&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <title>Amélia</title>
</head>

<body>

<!--Header-->
<div id="header-zone">
    <video autoplay muted loop id="header-video">
        <source src="assets/headerVideo.mp4" type="video/mp4">
    </video>

    <div class="header-text-zone">
        <img src="assets/headerText.png" id="head-text">    
    </div>

    <section class="scroll-down-zone">
        <a href="#" class="scroll-down" address="true"></a>
    </section>
    
</div>












    <a href="#foo-22">Go to foo</a>
    <a href="./page2.html">Go to page 2</a>

<% data.forEach(element => { %>

    <h2> Hello <%= element %> </h2>
    
<% }); %>

<% (new Array(100).fill("")).forEach((e,i) => { %>

    <h2 id="foo-<%=i%>"> Hello <%= i %> </h2>
    
<% }); %>





</body>

</html>

